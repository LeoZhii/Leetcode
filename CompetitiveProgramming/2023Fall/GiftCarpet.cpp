#include <stdio.h>
#include <iostream>
using namespace std;

int main()
{
    // store input
    char s;
    cin >> s;

    // store number of tests
    char tests = s[0];

    // iterate through each column, search for v,i,k,a
}